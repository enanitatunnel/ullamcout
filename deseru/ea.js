switch (someValue) {
  case 'enable':
    // Code to be executed if someValue matches 'enable'
    break;
  case 'disable':
    // Code to be executed if someValue matches 'disable'
    break;
  // Additional case statements can follow
  default:
    // Code to be executed if someValue doesn't match any of the cases
}
